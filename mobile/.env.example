# API URL for Expo Go (required on a physical phone; optional on iOS simulator)
# Use your computer's LAN IP — NOT localhost/127.0.0.1 on a real device.
# iOS simulator can reach the API at http://localhost:3001 when the server runs on the same Mac.
EXPO_PUBLIC_API_URL=http://192.168.1.100:3001
